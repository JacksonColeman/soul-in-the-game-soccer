export enum GameState{
    Start = "start",
    Settings = "settings",
    Selection = "selection",
    Season = "season",
    SeasonReview = "review",
    TransferWindow = "transfers",
}

export enum WeekState {
    Overview,
    PreMatch,
    PlayMatch,
    PostMatch,
  }