export enum GameState{
    Start = "start",
    Settings = "settings",
    Selection = "selection",
    Season = "season",
    SeasonRecap = "recap",
    TransferWindow = "transfers",
}

export enum WeekState {
    Overview,
    PreMatch,
    PlayMatch,
    PostMatch,
  }